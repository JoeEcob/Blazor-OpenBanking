{
  "name": "spendy-to-actual",
  "version": "1.0.0",
  "description": "Sync Spendy SQLite transactions to Actual Budget",
  "type": "module",
  "main": "sync.js",
  "scripts": {
    "sync": "node sync.js",
    "sync:reimport": "node sync.js --reimport-deleted",
    "dry-run": "node sync.js --dry-run"
  },
  "dependencies": {
    "@actual-app/api": "26.6.0",
    "better-sqlite3": "12.10.0"
  }
}
