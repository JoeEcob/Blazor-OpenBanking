info:
  name: Get Balance
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/accounts/{{accountId}}/balance"

  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"

    - name: Accept
      value: application/json
