info:
  name: Get Card By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/cards/{{cardId}}"

  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"

    - name: Accept
      value: application/json
