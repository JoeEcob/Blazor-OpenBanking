@page "/dashboard"

@inject DatabaseService DatabaseService
@inject BankingUpdateService BankingUpdateService

<div class="dashboard">
    @if (isLoading)
    {
        <p><em>Loading...</em></p>
    }
    else
    {
        <div class="add-new">
            <button href=@BankingUpdateService.GetAuthUrl()>Add new account</button>
        </div>

        <div class="flex-grid">
            <section class="dashboard-accounts">

                <h2>Accounts</h2>
                @foreach (var account in accounts)
                {
                    <div class="account clickable" @onclick="@(e => FetchTransactions(account.ProviderId, account.AccountId))">
                        <p>@account.DisplayName</p>
                        <p>Available: £@account.AvailableBalance</p>
                        <p>Current: £@account.CurrentBalance</p>
                        <p>Overdraft: £@account.Overdraft</p>
                    </div>
                }
            </section>

            <section class="dashboard-transactions">
                @if (transactions != null && transactions.Length > 0)
                {
                    <h2>Transactions</h2>
                    @foreach (var transaction in transactions)
                    {
                        <div class="transaction">
                            <span class="date" title="@transaction.Timestamp.ToString("G")">@transaction.Timestamp.ToString("d")</span>
                            <span class="description">@transaction.Description</span>
                            <span class="amount">£@transaction.Amount</span>
                        </div>
                    }
                }
            </section>
        </div>
    }
</div>

@code {
    private bool isLoading = true;
    private Account[] accounts;
    private Transaction[] transactions;

    protected override async Task OnInitializedAsync()
    {
        isLoading = false;
        accounts = DatabaseService.GetAccounts();
        transactions = new Transaction[0];

        // TODO - update if older than x date
        if (accounts == null || accounts.Length == 0)
        {
            await BankingUpdateService.FetchLatestAccountInfo();
            accounts = DatabaseService.GetAccounts();
        }

        if (accounts.Length > 0)
        {
            var firstAccount = accounts[0];
            await FetchTransactions(firstAccount.ProviderId, firstAccount.AccountId);
        }
    }

    private async Task FetchTransactions(LiteDB.ObjectId providerId, string accountId)
    {
        transactions = DatabaseService.GetTransactions(accountId);

        // TODO - update if older than x date
        if (transactions == null || transactions.Length == 0)
        {
            await BankingUpdateService.UpdateTransactions(providerId, accountId);
            transactions = DatabaseService.GetTransactions(accountId);
        }
    }
}
